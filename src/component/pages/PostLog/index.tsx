import React from 'react';
import {PostLogInfo} from '../../organisms'


/**
 * ReadMe.mdにも記載
 * Pageの役割は、作成したOrganismを流し込むのみ
 */

export default function PostLog({navigation, route}: any) {
  return (
    <PostLogInfo />
  );
}
